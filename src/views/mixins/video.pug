mixin video(videoData)

    div
        a(href=`/videos/${videoData.id}`) #{videoData.title}
        p=videoData.description
        small=videoData.createdAt
        br
        small 조회수 : #{videoData.meta.views}회
        hr